export const styles = (theme) => {
    const { miscellaneous: { spacing } } = theme;
    return ({
        container: {
            height: '100%',
            padding: spacing * 7
        }
    });
};
