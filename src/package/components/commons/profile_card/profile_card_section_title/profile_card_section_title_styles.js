export const styles = (theme) => {
    const { miscellaneous: { spacing } } = theme;
    return ({
        container: {
            color: 'inherit',
            fontWeight: 700,
            marginBottom: spacing,
            fontSize: 30,
            lineHeight: 1.4
        }
    });
};
