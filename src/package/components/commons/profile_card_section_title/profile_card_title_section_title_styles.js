export const styles = (theme) => {
    const { miscellaneous: { spacing } } = theme;
    return ({
        typography: {
            color: 'inherit',
            fontWeight: 700,
            marginBottom: spacing
        }
    });
};
