export const styles = (theme) => ({
    otherSkillsContainer: {
        width: '100%',
        margin: `${theme.miscellaneous.spacing * 3}px ${theme.miscellaneous.spacing * 5}px`
    },
    otherSkillsTitle: {
        marginBottom: theme.miscellaneous.spacing * 2
    }
});
